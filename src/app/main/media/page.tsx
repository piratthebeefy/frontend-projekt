import { Metadata } from 'next';

export const metadata: Metadata = {
    title: `Kultivátor: Média`,
    description: `Stránky pražské metalové kapely.`,
};

const Page = () => {
    return <p>Media page</p>;
};

export default Page;
